% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lower.R
\name{graph_to_tinycc_r_function}
\alias{graph_to_tinycc_r_function}
\title{Lower an anvl graph to a declared R function}
\usage{
graph_to_tinycc_r_function(graph)
}
\arguments{
\item{graph}{(\code{AnvlGraph})\cr A graph traced by \code{\link[anvl:trace_fn]{anvl::trace_fn()}}.}
}
\value{
A function whose body starts with \code{declare(type(...))}, suitable
for \code{\link[tccquickr:tccq_analyze]{tccquickr::tccq_analyze()}} and plain R evaluation alike.
}
\description{
Walks an \code{\link[anvl:AnvlGraph]{anvl::AnvlGraph}}'s calls in order and emits one declared R
function in tccquickr's input dialect. Each primitive is lowered by its
\code{tinycc} bridge rule when one claims the configuration, and by anvl's own
\code{quickr} rule otherwise, so the bridge only owns the primitives whose
quickr code would leave tccquickr's declared subset.
}
\details{
Graph constants become trailing formals; their traced values are attached
as the \code{anvltcc_constants} attribute so callers can complete the call.
}
